<?xml version="1.0" encoding="UTF-8"?>
<beans:beans
        xmlns="http://www.springframework.org/schema/security"
        xmlns:beans="http://www.springframework.org/schema/beans"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
                        http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security.xsd
                        ">

    <http use-expressions="true">
        <csrf disabled="true"/>
        <form-login
                login-page="/auth"
                authentication-failure-url="/auth/loginfail"
                always-use-default-target="true"
                authentication-success-handler-ref="authSuccessHandler"
                />
        <logout logout-success-url="/"/>
    </http>

    <authentication-manager>
        <authentication-provider >
            <jdbc-user-service
                    data-source-ref="dataSource"
                    users-by-username-query="select account_username, account_password,1 from account where account_username = ?"
                    authorities-by-username-query="select account_username, authority_role from authority where account_username = ?"/>
        </authentication-provider>
    </authentication-manager>

    <beans:bean id="authSuccessHandler" class="ru.todo100.activer.AuthSuccessHandler">
        <beans:property name="defaultTargetUrl" value="/profile"/>
    </beans:bean>

</beans:beans>